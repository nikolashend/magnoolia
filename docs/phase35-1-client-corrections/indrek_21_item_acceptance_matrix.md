# Indrek's 21 corrections — acceptance matrix

Round 1 of Phase 35.1. Every item is listed; none is silently skipped.

**Legend:** ✅ DONE · ⛔ BLOCKED (asset/access) · ❓ NEEDS CLIENT CONFIRMATION · ⏳ NOT STARTED (round 2)

| # | Client request | URL | Status | Action taken / why blocked | Editable in admin | Phase 36 ticket |
|---|---|---|---|---|---|---|
| 1 | Domain must not point at the old template page | all | ✅ | Handled in Phase 35.0 and verified live: `magnoolia.ee` → 302 → `magnoolia.estlanda.ee` (client's confirmed arrangement), canonical follows the serving host, no page emits an old-domain link. No Phase 35.1 change introduces one. | n/a | — |
| 2 | Estonian copy is not native | `/` + subpages | ✅ (3 named strings) / ⏳ (broader QA) | Applied Indrek's exact wording: hero H1 → **"Eksklusiivsed Magnoolia kodud Tallinna lähedal"**; "uusarenduse kindluse" → **"uusarenduse väärtuse"**; "Ridaelamu mugavus. Eramaja privaatsus." → **"…Eramaja kogemus."** Full 10-page ET sweep not yet done. | H1 + subheadline **YES**; why_title **NO** | Generic text override |
| 3 | Homepage image → evening LED facade | `/` | ✅ | Client supplied `materials/24.08.2026/Lisa Cam_20 lükandaken.jpg` (4500×2250). Installed as `Cam020.0000.jpg` + 480/768/1200/1600 webp variants. Homepage "19 kodu" block image swapped `Cam005` → **Cam020 (LED evening facade)**, alt: *Magnoolia ridaelamute õhtune fassaadivaade LED-valgustusega*. Note: the same render was already on site as gallery card 03, but only at 2000×1000 — now replaced by the 4500 px original. | NO | Media assignment |
| 4 | Photos 04/06/08 replaced; photo 09 title | `/` | ✅ title / ⏸ images | Title changed to **"Autovarjualune 2-le sõidukile"** (+ matching alt). **Image half deferred by client decision** (2026-08-24): which photos should replace 04/06/08 is unclear and needs Indrek's own choice. Current cards + the text each must support are documented in `ASSETS_NEEDED.md` §4. | NO | Homepage feature-card editor |
| 5 | Campaign/Eripakkumine self-editable | `/` | ✅ | **Found three disagreeing sources.** Homepage teaser read `config/magnoolia.php`; the red banner on `/kodud-ja-hinnad` was a hardcoded lang string with a fixed 20 000 € / 31.08.2026 offer; only the lower banner used the admin Campaign screen. So switching the campaign off in admin left the offer live on two pages. All three now read one normalised source in `MagnooliaPublicDataComposer`: **the admin Campaign screen is authoritative once anything has been published**; config is only a pre-first-publish fallback. | **YES** — Admin → Campaign → edit ET/RU/EN → Save draft → Preview → Validate → Publish | Campaign editor hardening (deadline/CTA fields) |
| 6 | Asukoht: drop duplicate button, embed map | `/asukoht` | ✅ (pins pending) | Top hero button kept. The duplicate *Ava aadress Google Mapsis* removed and replaced by a **live Google Maps embed** with the Magnoolia tee marker, language-aware (`hl=et/ru/en`), lazy-loaded, **no API key**. Also fixed a defect Indrek found while reviewing: the map buttons passed `Magnoolia+tee,Vaela,Kiili+vald` unencoded, which Google could not resolve — it opened a map with no marker. All map links now share one encoded address via `$mgMapUrl` and Google's documented key-free `/maps/search/?api=1` format; verified the URL resolves. The 6 nearby objects are offered as individual map links under the map. **Putting them as pins on the same embed needs client input** — a keyless embed shows only one marker. See `MAP_PINS_REQUEST.md`. | NO (My Maps would make it client-editable) | Location map/pins editor |
| 7 | Asukoht: high-res architect plan + Opmani tee | `/asukoht` | ✅ | Client supplied `materials/24.08.2026/MAGNOOLIA_ASENDIPLAAN.pdf` (19×27 inch). Rasterised via pdf.js at scale 2.2 → 3025×4241, published as `asendiplaan-hires.webp` (2400 px) + 1200/768 variants. New section **"Arhitekti asendiplaan"** on `/asukoht` above the distance table, click opens full size in a new tab. **"OPMANI TEE" label is visible** on the drawing, as Indrek marked. Alt: *Magnoolia arenduse arhitekti asendiplaan Opmani tee tähisega*. Interactive masterplan on `/kodud-ja-hinnad` untouched. | NO | Media assignment |
| 8 | Gallery: LED evening photos earlier | `/galerii` | ⏳ | Gallery order lives in the publication payload; needs the payload-patch route documented in project memory. | PARTIAL | Gallery drag-sort |
| 9 | Eripakkumine on Hinnad page | `/kodud-ja-hinnad` | ✅ | The red uppercase banner now renders the admin campaign text and **disappears when the campaign is switched off**. It previously advertised a fixed 20 000 € offer expiring 31.08.2026 that nobody could edit or retire — a week before expiry at the time of this phase. | **YES** — same Campaign screen as item 5 | Campaign editor hardening |
| 10 | Lisateenused: bigger image, always visible | `/sisedisain` | ✅ | The proof sheet (incl. *Elektri- ja nutiseadmed*) sat in a half-width grid column at the 768 px variant — unreadable for a sales block. It now spans the **full card width** below the item list and serves the **1400 px** variant with srcset. Combined with item 18 it is visible without any click. | list/labels **NO** (`config/magnoolia_interiors.php`) | Extras structured editor |
| 11 | Add hooviala; "Netopind" → "Netopind kokku" | `/kodud-ja-hinnad` | ✅ | Implemented as Indrek annotated (client approved option A): unit modal now shows **Köetav pind** (net_area) → **Panipaiga pind** (storage_area) → **Netopind kokku** (computed sum, e.g. 129,6 + 7,3 = 136,9), then Terrass, Rõdu, **Hooviala rounded to a whole number** (959,7 → 960 m²). `Netopind kokku` is omitted entirely when either part is missing — no invented total. Plan A/B cards relabelled `Netopind` → **Köetav pind** in ET/RU/EN, since they show net_area alone. | values **YES** (Units); labels **NO** | Generic text override |
| 12 | 143,2 m² 5-room variant does not exist | `/kodud-ja-hinnad`, KKK, schema | ✅ | **Confirmed from the live publication payload: no unit has 143,2 m².** Actual: type-a = 4 rooms 129,2 m² (7 homes), type-b = 5 rooms 129,6 m² (12 homes). Removed the figure from ET/RU/EN lang files, the plan cards, the page JSON-LD and the FAQPage schema — 34 replacements. **Pesuruumide arv added after client confirmation: Plan A = 3, Plan B = 2**, with a new `spec_washrooms` label in ET/RU/EN. | NO | Plan type editor + FAQ editor |
| 13 | Arhitektuur: add missing renders | `/arhitektuur` | ✅ | Both supplied renders added to **VÄLISVAATED**: `Cam020.0000.jpg` (LED evening facade with terrace sliding door) and `Cam007.0000.jpg` (evening view from the yard side, new to the site). No duplicates — neither was in the exterior list before. Both optimised to webp variants by the existing pipeline. | NO | Architecture renders editor |
| 14 | Exterior element photos bigger + "Külm panipaik" | `/arhitektuur` | ⏳ / ❓ | Not started. The "külm panipaik" copy about a key system is still unverified — must not be invented. | NO | Generic text + media |
| 15 | Sisedisain CTAs must reach Diana | `/sisedisain` | ✅ | **Traced the full chain:** CTA → inquiry drawer → `POST /kontakt` → `MagnooliaController` → `config('magnoolia.project.contact_email')` = **diana@estlanda.ee**, with `source_component` stored on the lead. Two real defects fixed: *Täpsusta viimistlust* was a bare link to `/kontakt` carrying no context (lead arrived unattributed) — it now opens the same drawer tagged `sisedisain_viimistlus`, with the href kept as a no-JS fallback so it cannot dead-end; and the finish-package CTAs still identified themselves as `ehitusinfo_siseviimistlus` although the block moved to `/sisedisain`, so leads were mislabelled in the Leads admin — now `sisedisain_viimistluspakett`. | recipient **NO** (config) | Lead recipient/routing config |
| 16 | Remove self-linking "Sisedisain" button | `/sisedisain` | ✅ | Removed the dark strip with the gold **"Sisedisain →"** button (85→0 refs). Confirmed safe: `material-specs-section` is included **only** on `/sisedisain`, so the button always pointed at the page the visitor was already on. The useful CTAs (contact / gallery) below are untouched, and no empty strip remains. | NO | — |
| 17 | Remove duplicate Prestige section | `/sisedisain` | ✅ | Removed the 85-line **"Prestige pakett / Rahulik ja kvaliteetne kodutunne"** teaser (`id="prestige"`) — exactly the block Indrek crossed out. Verified first that nothing linked to its `#prestige` anchor and that the detailed specs below survive: *Elektri- ja nutiseadmed* ✓, *Sanitaartehnika* ✓, one H1 ✓. Backup of the pre-edit file kept. | NO | — |
| 18 | Accordions open by default; big black photo | `/sisedisain` | ✅ | All **5 of 5** category cards now render `<details … open>` (was: first card only). Buyers told Indrek they could not find the specification behind the "+". Native `<details>` keeps the toggle and its accessibility — only the initial state changed, so a visitor can still collapse a card. The black proof sheet is large by default via item 10. | NO | — |
| 19 | How to replace black-background photos | `/sisedisain` | ✅ (answered) | Paths live in `config/magnoolia_interiors.php` (`*-overview.webp`), regenerated by `npm run magnoolia:generate:interior` from `materials/phase31/*.jpg`. **Not admin-editable today** — exact answer, not a vague claim. | NO | Sisedisain section image editor |
| 20 | Add Kakumäe to Arendaja | `/arendajast` | ✅ **already present** | `Kakumäe Residence` → `https://kakumae.com/` is already the third card in *Teised Estlanda projektid*, with `kakumae.webp`, `target="_blank"` and `rel="noopener noreferrer"`. Verified rendering live. **No change needed** — reported rather than invented work. | NO | Developer projects editor |
| 21 | Vary subpage header photos | all subpages | ⏳ | Audit not started. | NO | Per-page header image editor |

## Round-1 totals

| Status | Count | Items |
|---|---|---|
| ✅ Done | 17 | 1, 2 (named strings), 3, 4 (title), 5, **6**, 7, 9, 10, 11, 12, 13, 15, 16, 17, 18, 19, 20 |
| ⏸ Deferred by client | 1 | image half of 4 — Indrek must choose the replacements |
| ❓ Needs client decision | 1 | 14 (külm panipaik copy) |
| ⏳ Remaining | 3 | **8** (gallery order), **14** (exterior photos), **21** (header images) |
| 🔵 Awaiting client | 1 | item 6 — multiple map pins, needs a My Maps link or an API key (`MAP_PINS_REQUEST.md`) |

## Assets received 2026-08-24

| File | Used for | Result |
|---|---|---|
| `Lisa Cam_20 lükandaken.jpg` (4500×2250) | items 3, 13 | → `Cam020.0000.jpg` + 4 webp variants |
| `Cam007.0000.jpg` (4500×2250) | item 13 | → `Cam007.0000.jpg` + 4 webp variants |
| `MAGNOOLIA_ASENDIPLAAN.pdf` (19×27 in) | item 7 | → `asendiplaan-hires.webp` 2400 px + 2 variants |
